# CLAUDE.md

이 파일은 이 프로젝트에서 AI(Claude Code)가 지켜야 할 규칙과 프로젝트 맥락을 담고 있습니다.
**AI에게: 코드를 작성하거나 수정하기 전에 이 파일 전체를 읽고, 아래 규칙을 항상 따르세요.**

---

## 🚀 시작하는 법 (학생용 — 이것부터 읽으세요)

1. 이 `CLAUDE.md` 파일을 프로젝트 **최상단 폴더**(package.json이 있는 곳)에 둡니다.
   → 이러면 Claude Code가 시작할 때 자동으로 이 파일을 읽습니다. 따로 "기억해줘"라고 안 해도 됩니다.
2. Claude Code를 켜고, **아래 문장을 그대로 복사해서 붙여넣으세요.**

```
안녕! 이 프로젝트의 CLAUDE.md를 먼저 읽어줘.
그다음 10번 항목(우리 팀이 정한 것)을 나랑 같이 채우자.
다 채우면, 뭘 먼저 만들지 제안해줘. 나는 코딩을 처음 해.
```

3. 작업 중간중간, 중요한 걸 만들기 전에 이렇게 한 번씩 상기시키면 좋아요:
   → "CLAUDE.md 규칙 기억하지? 한 번에 조금씩, 쉬운 말로 부탁해."

> 💡 팁: 10번 빈칸을 채우지 않으면 AI가 "무엇을 만드는지" 모릅니다. **가장 먼저 채우세요.**

---

## 1. 프로젝트 개요

- **무엇을 만드는가**: 지역 어르신의 건강·돌봄 문제를 돕는 AI 웹 서비스
- **누가 만드는가**: 코딩을 처음 하는 고등학생 팀 (해커톤)
- **목표**: 인터넷에 배포되어 링크로 접속 가능하고, 핵심 기능 1개가 확실히 작동하는 웹앱
- **중요**: 완벽함보다 **작동하는 것**이 우선. 복잡한 기능보다 단순하고 확실한 기능을 선호한다.

> 이 서비스는 의료 **진단·치료를 대신하지 않는다.**
> 기록, 정리, 알림, 안내, 연결처럼 **돕는 역할**만 수행한다.
> 건강 관련 화면에는 "이 서비스는 의료 조언을 대신하지 않으며, 정확한 진단은 의료진과 상담하세요"라는 안내 문구를 넣는다.

---

## 2. 기술 스택 (이 스택에서 벗어나지 말 것)

- **프레임워크**: Next.js (App Router) + React
- **언어**: TypeScript
- **스타일**: Tailwind CSS
- **데이터베이스 / 인증**: Supabase
- **배포**: Vercel (GitHub 연동) — 가능하면 MCP, 안 되면 브라우저로 AI가 대신 처리 (8번 참고)
- **AI 기능**: API 방식으로 호출 (키는 환경변수로 관리)

**금지**: 위에 없는 새로운 프레임워크나 무거운 라이브러리를 임의로 추가하지 말 것.
꼭 필요하면 먼저 사람에게 "이걸 추가해도 될까요?"라고 물어보고, 이유를 한 줄로 설명한다.

---

## 3. 기획(PRD)부터 시작하기

코드를 짜기 전에 **무엇을 만들지 먼저 정리(PRD 작성)**한다. AI는 사람이 곧바로 코딩으로 넘어가려 하면 "먼저 뭘 만들지 짧게 정리하고 시작하는 게 좋아요"라고 안내한다.

### 3-1. PRD 작성 방법

1. **1순위 — ManyFast MCP가 연결되어 있으면 그것으로 PRD를 작성한다.**
   - ManyFast MCP는 이 프로젝트의 **PRD 작성 프레임워크**다. 연결되어 있으면 이 도구를 사용해 기획을 정리한다.
   - 시작할 때 "ManyFast MCP가 연결되어 있는지" 확인하고, 있으면 이 방법을 우선한다.

2. **2순위 — 없으면 아래 항목을 간단히 채우는 것으로 대신한다.**
   - 도우려는 대상 / 문제 / 서비스 이름 / 핵심 기능 1개 / AI의 역할
   - (10번 섹션의 빈칸이 이 최소 PRD에 해당한다.)

### 3-2. PRD를 짧고 실행 가능하게

- 초보자·해커톤 상황이므로 PRD는 **길게 쓰지 않는다.** 핵심만 담는다.
- "이 기능을 만들면 어르신에게 뭐가 좋아지는가"가 각 기능마다 한 줄로 설명되게 한다.
- 기능이 많아지면 "핵심 1개부터 만들고 나머지는 나중에"로 범위를 줄이도록 조언한다.

---

## 4. AI에게 주는 작업 규칙 (가장 중요)

이 프로젝트를 다루는 사람은 **코딩 초보자**다. 다음을 반드시 지킨다.

1. **항상 한국어로 설명한다.** 코드 주석도 한국어로 단다.
2. **전문 용어를 쓰면 곧바로 쉬운 말로 풀어준다.** (예: "환경변수(비밀번호 같은 설정값)")
3. **파일을 만들거나 고칠 때는 "이 파일을 어디에 저장해야 하는지" 경로를 명확히 알려준다.**
4. **한 번에 조금씩** 진행한다. 거대한 코드를 한꺼번에 쏟지 말고, 단계별로 나눠서 "이걸 먼저 해보고 되는지 확인하세요"라고 안내한다.
5. **터미널 명령어**를 시킬 때는 복사해서 붙여넣을 수 있게 코드 블록으로 주고, 그 명령이 무엇을 하는지 한 줄로 설명한다.
6. 코드를 준 뒤에는 **"이제 무엇을 확인하면 되는지"**(예: 브라우저에서 뭐가 보여야 하는지)를 알려준다.
7. 에러가 나면, 겁주지 말고 **"이 에러는 보통 이런 이유예요"**라고 원인부터 짚어준 뒤 해결 방법을 준다.
8. **비밀 키(API Key, Supabase Key 등)를 코드에 직접 적지 않는다.** 반드시 환경변수(`.env.local`)로 처리하고, 그 방법을 알려준다.

---

## 5. 화면·디자인 원칙 (어르신 사용자 고려)

- **글씨를 크게.** 기본 본문은 최소 18px 이상, 버튼 글씨는 더 크게.
- **버튼을 크고 명확하게.** 손가락으로 누르기 쉬운 크기.
- **색 대비를 뚜렷하게.** 흐린 회색 글씨는 피한다.
- **화면은 단순하게.** 한 화면에 정보를 너무 많이 넣지 않는다.
- **어려운 말 대신 쉬운 말.** "제출" 대신 "저장하기"처럼 직관적인 단어.

---

## 6. 프로젝트 구조 (기본 틀)

```
/app          → 페이지 화면들 (Next.js App Router)
/components   → 재사용하는 화면 조각 (버튼, 카드 등)
/lib          → Supabase 연결 등 도우미 코드
.env.local    → 비밀 키를 넣는 곳 (절대 GitHub에 올리지 않음)
```

- 새 화면이 필요하면 `/app` 아래에 폴더를 만든다.
- Supabase 연결 코드는 `/lib/supabase.ts` 한 곳에 모은다.

---

## 7. Supabase 사용 규칙

- 테이블(데이터를 넣는 표)을 만들 때는 **왜 이 테이블이 필요한지** 먼저 한 줄로 설명한다.
- 데이터를 저장·불러오는 코드는 최대한 단순하게. 복잡한 쿼리는 피한다.
- 로그인 기능은 **꼭 필요할 때만** 넣는다. (해커톤에선 없어도 됨. 시간을 아낀다.)
- Supabase URL과 Key는 `.env.local`에 넣고, 코드에서는 환경변수로 불러온다.

---

## 8. 배포(Vercel · Supabase) 규칙

배포와 설정 작업은 **가능한 한 AI가 대신 처리**한다. 아래 순서로 시도한다.

### 7-1. 배포·설정을 처리하는 순서 (폴백 3단계)

1. **1순위 — MCP 연결이 있으면 MCP로 처리한다.**
   - Supabase MCP나 Vercel MCP가 연결되어 있으면, 그 도구를 사용해 프로젝트 생성·테이블 생성·환경변수 설정·배포를 직접 진행한다.
   - 시작할 때 "Supabase/Vercel MCP가 연결되어 있는지" 먼저 확인하고, 있으면 이 방법을 쓴다.

2. **2순위 — MCP가 없으면 브라우저로 대신 처리한다.**
   - 브라우저 도구(예: Claude in Chrome)를 쓸 수 있으면, Supabase·Vercel 사이트를 직접 열어서 설정과 배포를 대신 진행한다.
   - 단, **로그인·비밀번호 입력·결제·계정 생성처럼 사람이 직접 해야 하는 단계**는 대신하지 않는다. 그 지점에서는 멈추고 "여기는 직접 해주세요"라고 사람에게 넘긴다.

3. **3순위 — 둘 다 안 되면 사람이 직접 하도록 안내한다.**
   - MCP도 브라우저도 못 쓰면, 사람이 따라 할 수 있게 **화면 순서·클릭 위치를 단계별로** 쉬운 말로 알려준다.

> AI에게: 위 순서를 **말로 설명만 하지 말고 실제로 시도**한다.
> "MCP가 연결되어 있나요? 없으면 제가 브라우저로 해볼게요"처럼 먼저 확인하고 움직인다.

### 7-2. 배포 공통 규칙 (어떤 방법이든)

- 기본 배포 방식은 **GitHub에 코드를 올리면 Vercel이 자동으로** 배포하는 것이다.
- 배포가 안 될 때 가장 흔한 원인은 **환경변수 누락**이다.
  → `.env.local`에 넣은 모든 키를 **Vercel의 Environment Variables에도 똑같이** 넣어야 한다. MCP·브라우저로 처리할 때도 이 과정을 빠뜨리지 않는다.
- 비밀 키를 다룰 때는 **값을 채팅창에 그대로 노출하지 않도록** 주의하고, 안전하게 설정하는 방법을 안내한다.
- 배포가 끝나면 실제 링크(`https://...vercel.app`)로 접속해 작동하는지 확인하라고 안내한다.

---

## 9. 우선순위 (시간이 부족할 때)

AI는 아래 순서로 도와준다. 사람이 욕심을 내면 "먼저 핵심부터 완성하는 게 좋아요"라고 조언한다.

1. **반드시**: 링크로 접속되는 화면 1개 + 핵심 기능 1개 작동
2. **되면 좋음**: 데이터 저장(Supabase), 기본 디자인 정리
3. **여유되면**: 로그인, 추가 화면, 부가 기능

> "기능 3개가 반쯤 되는 것"보다 **"기능 1개가 확실히 작동하는 것"**을 항상 우선한다.

---

## 10. 이 프로젝트에서 우리 팀이 정한 것 (직접 채우기)

> 아래는 팀이 시작할 때 직접 적어서 AI에게 알려주는 칸입니다.

- 우리가 돕는 대상: <!-- 예: 혼자 사시는 70대 어르신 -->
- 우리가 푸는 문제: <!-- 예: 약 먹는 시간을 자꾸 잊는 문제 -->
- 우리 서비스 이름: <!-- 예: 약똑똑 -->
- 핵심 기능 1개: <!-- 예: 약 이름과 시간을 등록하면 알림을 주는 기능 -->
- AI가 하는 역할: <!-- 예: 약 사진을 읽어서 이름과 복용법을 자동 정리 -->

---

## 11. 사람이 막혔을 때 AI가 해줄 것

- 에러 메시지를 붙여넣으면 → 원인을 쉬운 말로 설명하고 해결책을 단계별로 준다.
- "이거 왜 안 돼요?"라고 물으면 → 혼내지 말고, 확인할 것들을 체크리스트로 준다.
- 사람이 잘못된 방향으로 가면 → 부드럽게 더 쉬운 대안을 제안한다.

---

**AI에게 마지막 당부: 이 팀은 처음 만드는 사람들입니다. 친절하게, 천천히, 한 걸음씩 도와주세요.**
